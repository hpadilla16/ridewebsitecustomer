export function generateMetadata({ params }) {
  return {
    title: 'Rental Vehicle',
    description: 'View this rental vehicle class on Ride Car Sharing. See pricing, specs, and reserve your car online.',
  };
}
export { default } from '../../../site/rent/[vehicleTypeId]/page';
