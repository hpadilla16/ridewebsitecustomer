export function generateMetadata({ params }) {
  return {
    title: 'Host Profile',
    description: 'View this host on Ride Car Sharing — their vehicles, ratings, and guest reviews.',
  };
}
export { default } from '../../../site/hosts/profile';
